# BMO — Codex 桌面助理工作清单

硬件采购与分阶段下单建议见：[CODEX_DESK_ASSISTANT_BOM_ZH.md](./CODEX_DESK_ASSISTANT_BOM_ZH.md)。

## 最终目标

做出一台可以放在工位上的实体桌面助理：

- 无需按键即可中英文唤醒和连续对话；
- 普通问题由实时 GPT 语音回答；
- 编码指令交给 Codex 在指定项目中执行；
- 日程、任务、消息等办公指令调用飞书；
- 能在用户回到工位、会议开始前和 DDL 临近时主动说话；
- Orange Pi、Tuya T5AI 归还后，可以在标准 Linux 主机上重新部署。

## 角色标记

- **FE**：前端、屏幕交互、动画和状态可视化
- **VOICE**：唤醒、实时音频、降噪、回声消除、语音模型
- **BRAIN**：意图路由、Codex、GPT、工具调用和权限
- **LARK**：飞书日历、任务、消息和提醒
- **HW**：主机、麦克风、扬声器、屏幕和在席传感器

---

## P0：确定技术方案与预算

- [x] **HW** 主板固定为 Radxa ZERO 3W 2GB（无 eMMC）。
- [x] **HW/FE** 使用 3.5 英寸 HDMI 小屏作为 BMO 面部显示，先借用现有
  HDMI 显示器验证前端后再下单小屏。
- [x] **VOICE/HW** 全双工版本使用 ReSpeaker Lite USB 两麦阵列；极限省钱
  原型才使用普通 USB 麦克风并接受半双工。
- [ ] **HW** 在席检测优先使用电脑解锁/空闲恢复或手机蓝牙靠近；误判无法
  接受时才购买毫米波传感器。
- [ ] **VOICE** 确定实时语音供应商和部署地区。
  - OpenAI API 可用地区：优先测试 Realtime API。
  - 中国大陆部署：评估腾讯 TRTC AI 实时对话、火山引擎实时语音或其他合规服务。
- [ ] **VOICE** 确定语音模式。
  - 方案 A：实时语音模型直接处理普通问答，Codex 处理 coding/action。
  - 方案 B：实时服务只负责 ASR/TTS，所有文本都交给 Codex。
- [ ] **BRAIN** 设定每月 API 预算、单次会话时长和用量上限。
- [ ] **全员** 确认隐私策略：唤醒前音频不上传，工作消息只允许限定关键词/群聊。

### P0 验收

- 有一张最终架构图。
- 有明确的设备采购清单。
- 有语音服务账号、测试额度和月度费用上限。
- 明确谁负责 FE、VOICE、BRAIN、LARK、HW。

---

## P1：跑通免按键双语语音

- [x] **VOICE** Vosk 本地唤醒词检测。
- [x] **VOICE** whisper.cpp 中英文自动转写草稿。
- [x] **VOICE** 本地中英文 TTS 草稿。
- [x] **VOICE** TTS 播放期间持续读取 AEC 音频流，并保留插话开头。
- [ ] **VOICE** 接入 WebRTC 或双向 WebSocket。
- [ ] **VOICE/HW** 接入 AEC 回声消除，避免 BMO 听到自己说话。
- [ ] **VOICE/HW** 接入降噪、自动增益和麦克风阵列波束成形。
- [ ] **VOICE** 接入 VAD/语义 VAD，判断用户何时说完。
- [x] **VOICE** 已实现可配置 barge-in；待真机调节能量阈值与连续块数。
- [ ] **VOICE** 处理中文、英文和中英混说。
- [ ] **FE** 显示实时字幕和用户当前输入。

### P1 验收

- 唤醒后首个可听响应尽量控制在 1.5 秒内。
- 用户能在 BMO 说话时插话并成功打断。
- 扬声器以正常音量播放时不会造成连续误唤醒。
- 中文、英文、中英混说各完成 20 轮测试。

---

## P2：实现 question / coding / action 三路大脑

- [x] **BRAIN** 已建立保守意图路由草稿。
- [x] **BRAIN** 默认将不明确的输入归为 `question`。
- [x] **BRAIN** 已建立持久 Codex thread 草稿。
- [ ] **BRAIN** 将路由升级为“规则优先 + 模型复核”，并保留只读默认值。
- [ ] **BRAIN** 建立统一请求结构：
  - transcript
  - language
  - intent
  - confidence
  - project
  - requires_confirmation
- [ ] **BRAIN** `question`：交给实时 GPT，禁止文件修改和工具调用。
- [ ] **BRAIN** `coding`：调用 Codex，并限制在项目白名单目录。
- [ ] **BRAIN** `action`：调用飞书或设备功能，并执行权限检查。
- [ ] **BRAIN** 低置信度时先追问，不直接执行。
- [ ] **BRAIN** 支持上下文指令，例如“那你直接改吧”延续上一轮项目。
- [ ] **BRAIN** 建立会话记忆和长期偏好，但不保存敏感语音原始文件。

### P2 验收

- 使用至少 100 条中文、英文和混合指令制作路由测试集。
- question/coding/action 误执行率为 0。
- coding 指令无法写入项目白名单以外的目录。
- 普通问题不会触发 shell、飞书写入或文件修改。

---

## P3：Codex 语音 Vibe Coding

- [ ] **BRAIN** 通过语音选择或确认项目。
- [ ] **BRAIN** 将长时间 coding 任务改成后台 Job，而不是阻塞语音对话。
- [ ] **BRAIN** Codex 开始工作后返回 Job ID 和预计状态。
- [ ] **FE** 显示 `inspecting / coding / testing / done / failed`。
- [ ] **VOICE** 工作期间可以回答“做到哪了”。
- [ ] **BRAIN** 完成后自动运行项目测试和静态检查。
- [ ] **VOICE** 完成时主动播报摘要。
- [ ] **FE** 显示改动文件、测试结果和需要确认的后续操作。
- [ ] **BRAIN** push、deploy、删除和外部发布必须单独确认。
- [ ] **BRAIN** 支持取消正在运行的 Codex Job。

### P3 验收

- 能通过语音完成一个小型前端修改并运行测试。
- 编码期间语音对话不会完全失去响应。
- 用户可以查询进度、取消任务和听取最终结果。
- 未确认时不会自动 push 或 deploy。

---

## P4：飞书办公能力

- [x] **LARK** 飞书日历只读查询适配器草稿。
- [x] **LARK** 未完成任务读取草稿。
- [x] **LARK** 会前 5 分钟提醒和去重草稿。
- [x] **LARK** 工作消息限定关键词读取草稿。
- [ ] **LARK** 在最终 `bmo` 系统用户下完成飞书用户授权。
- [ ] **LARK** 查询今天、明天和未来 7 天日程。
- [ ] **LARK** 创建、修改和取消个人日程。
- [ ] **LARK** 查询、创建和更新飞书任务。
- [ ] **LARK** 提取工作消息中的负责人、DDL 和依赖关系。
- [ ] **LARK** 对候选时间、参会人、会议室和模糊日期进行二次确认。
- [ ] **LARK** 高风险确认触发后，通过语音读出风险并等待明确同意。
- [ ] **LARK** 建立幂等键，避免语音重试导致重复创建会议或任务。

### P4 验收

- “我今天有什么安排”可以正确播报。
- “明天下午三点创建产品会”会先补齐缺失信息，再创建一次。
- 网络重试不会创建重复会议。
- 未授权的消息范围无法被搜索。

---

## P5：主动助理与自动排程

- [x] **BRAIN/LARK** 到岗后主动简报逻辑草稿。
- [x] **BRAIN/LARK** 每日 DDL 规划草稿。
- [x] **BRAIN/LARK** 自动写入默认关闭。
- [ ] **HW** 在席传感器写入稳定的 `present/absent` 状态。
- [ ] **BRAIN** 设置离席阈值和播报冷却时间。
- [ ] **BRAIN** 到岗后汇总下一场会议、重要 DDL、冲突和建议动作。
- [ ] **LARK** 每日固定时间生成未来 7 天工作计划。
- [ ] **LARK** 根据工作量、依赖和已有会议寻找专注时间。
- [ ] **FE** 显示“建议计划”和“确认写入”按钮。
- [ ] **LARK** 自动排程开启后，只创建无参会人的 `[BMO专注]` 时间块。
- [ ] **VOICE** 夜间、会议中和免打扰状态不主动播报。
- [ ] **BRAIN** 支持“今天别提醒我”和“推迟 10 分钟”等临时偏好。

### P5 验收

- 离岗再返回只播报一次，不会因走动反复触发。
- 会议提醒误差不超过 1 分钟。
- 自动规划不会覆盖已有会议。
- 默认状态下只给建议，不直接写入日历。

---

## P6：前端和 BMO 表情系统

- [x] **FE** 已保留 `idle/listening/thinking/speaking/error` 状态接口。
- [ ] **FE** 增加 `wake/interrupted/tool_call/coding/reminder` 状态。
- [ ] **FE** 制作待机表情、聆听动画、思考动画和说话口型。
- [ ] **FE** 增加实时字幕层。
- [ ] **FE** 增加意图标识：问答、编码、办公操作。
- [ ] **FE** 增加高风险操作确认卡片。
- [ ] **FE** 增加 Codex Job 进度页。
- [ ] **FE** 增加今日日程和 DDL 卡片。
- [ ] **FE** 增加网络、麦克风、Codex 和飞书连接状态。
- [ ] **FE** 将原 Tuya 页面需要保留的视觉资产迁移到板卡无关前端。

### P6 验收

- 所有语音状态在 200ms 内反映到屏幕。
- 用户可以看见系统判断的是 question、coding 还是 action。
- 所有外部写入操作都有屏幕或语音确认反馈。
- 前端可以在 Raspberry Pi 和普通电脑浏览器运行。

---

## P7：安全、稳定性与部署

- [x] **BRAIN** 子进程不通过 shell 拼接命令。
- [x] **BRAIN** question 模式使用只读 Codex sandbox。
- [x] **BRAIN** 自动排程默认只生成草案。
- [ ] **全员** 为 API Key、Codex 登录和飞书令牌使用系统密钥存储。
- [ ] **BRAIN** 增加 API 用量和每日费用上限。
- [ ] **BRAIN** 增加断网、本地降级和重连策略。
- [ ] **VOICE** 断网时仍可唤醒并播报“当前离线”。
- [ ] **BRAIN** 增加 Codex 超时、取消和进程恢复。
- [ ] **LARK** 增加飞书令牌过期提醒。
- [ ] **HW** 制作一键安装和系统镜像备份。
- [ ] **HW** systemd 开机自启和进程健康检查。
- [ ] **全员** 完成隐私提示、日志脱敏和数据保留周期。

### P7 验收

- 断网、飞书过期、Codex 超时、模型限流都能恢复。
- 日志中没有 API Key、完整飞书令牌和不必要的消息正文。
- 重启设备后服务自动恢复，且不会重复创建任务或日程。
- 连续运行 24 小时无崩溃和明显内存增长。

---

## 推荐开发顺序

### Sprint 1：可自然说话

1. 采购或确定主机、麦克风和扬声器。
2. 确定实时语音服务。
3. 完成 WebRTC/双向流、AEC、VAD 和打断。
4. 完成中英文 20 轮语音测试。

### Sprint 2：可以做事

1. 完成 question/coding/action 路由。
2. 将 Codex coding 改为后台 Job。
3. 接通飞书日历和任务。
4. 完成确认、取消和幂等机制。

### Sprint 3：像工位助理

1. 接入在席检测。
2. 完成到岗简报、会前提醒和 DDL 规划。
3. 完成前端状态、动画、字幕和日程卡片。
4. 做 24 小时稳定性与误触发测试。

## 当前下一步

最先执行以下五项：

1. [ ] 选择最终实时语音供应商。
2. [ ] 采购或借到 ReSpeaker Lite USB 两麦阵列。
3. [ ] 在 Radxa 真机上调节 barge-in 阈值并完成 20 轮插话测试。
4. [ ] 将 Codex coding 改成后台 Job。
5. [ ] 在最终设备用户下完成 Codex 和飞书授权。
